server {
    listen 80;

    # Serve React App
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Optional: Proxy API requests to backend (if needed later)
    # location /api/ {
    #     proxy_pass http://backend:8080;
    # }
}
